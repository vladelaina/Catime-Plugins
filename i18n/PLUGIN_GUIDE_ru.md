# Руководство по плагинам Catime

## Что такое плагин?

Плагин — это файл скрипта, который отображает пользовательский контент в окне Catime. Например:

- 📺 Статистика ваших видео на Bilibili/YouTube
- 📈 Индексы NASDAQ и S&P 500 в реальном времени
- 🌤️ Местный прогноз погоды
- 🌐 Статистика посещаемости вашего сайта
- 💻 Состояние сервера
- ……

**Основная идея: Любые данные, которые может получить ваш скрипт, могут отображаться в окне Catime!**

Кроме того, эти данные можно разместить в любом месте экрана и масштабировать до любого размера, как и отображение времени Catime — всегда видимые, не перекрывая другие окна.

**Как это работает:** Ваш скрипт записывает в `output.txt` → Catime читает → Отображает в окне. Всё просто!

> **Совет:** Убедитесь, что у вас установлена необходимая среда выполнения (например, Python, Node.js и т.д.)

---

## Быстрый старт за 30 секунд

Не хотите писать код? Сначала попробуйте вручную:

### Шаг 1: Откройте папку плагинов

Правый клик на значке Catime → `Плагины` → `Открыть папку плагинов`

### Шаг 2: Отредактируйте output.txt

Найдите (или создайте) `output.txt` в папке и напишите что-нибудь:

```
Привет, Catime!
Это моё первое сообщение 🎉
```

### Шаг 3: Отобразите содержимое файла

Правый клик на значке Catime → `Плагины` → `Показать файл плагина`

**Готово!** Окно Catime теперь показывает ваш контент.

> В этом суть плагинов: **Что вы пишете в output.txt, то появляется в окне**.
> Скрипты плагинов просто автоматизируют этот процесс.

---

## Создайте свой первый плагин за 3 шага

### Шаг 1: Откройте папку плагинов

Правый клик на значке Catime → `Плагины` → `Открыть папку плагинов`

### Шаг 2: Создайте файл скрипта

Создайте новый файл в этой папке, например, `hello.py`:

```python
with open('output.txt', 'w', encoding='utf-8') as f:
    f.write('Привет, Catime!')
```

**Всего несколько строк!**

### Шаг 3: Запустите плагин

1. Правый клик на значке Catime
2. `Плагины` → Нажмите на `hello.py`
3. В первый раз спросит, доверяете ли вы, нажмите "Доверять и запустить"

**Готово!** Окно теперь показывает "Привет, Catime!"

---

## Ключевой момент

Что ваш скрипт записывает в `output.txt`, то Catime отображает. Отображение автоматически обновляется при обновлении файла.

---

## Специальные теги (Опционально)

Используйте эти теги при необходимости:

| Тег | Функция | Пример |
|-----|---------|--------|
| `<md></md>` | Включить форматирование Markdown | `<md>**жирный** *курсив*</md>` |
| `<catime></catime>` | Показать время таймера | `Работает <catime></catime>` → `Работает 00:05:30` |
| `<exit>N</exit>` | Автоматически закрыть плагин через N секунд | `<exit>5</exit>` → закроется через 5 секунд |
| `<fps:N>` | Обновлять N раз в секунду (по умолчанию 2, диапазон 1-100) | `<fps:10>` → 10 обновлений в секунду |
| `<color:значение></color>` | Установить цвет текста (поддерживает градиенты) | `<color:#FF0000>красный</color>` |
| `<font:путь></font>` | Установить шрифт (путь к файлу шрифта) | `<font:C:\Windows\Fonts\comic.ttf>весело</font>` |
| `![](путь)` | Показать изображение (локальный путь или URL) | `![](погода.png)` или `![](https://example.com/img.png)` |
| `![ШxВ](путь)` | Показать изображение определённого размера | `![100x50](logo.png)` или `![200](logo.png)` (только ширина) |

> **О `<fps:N>`:** По умолчанию обновление каждые 500мс (2 раза в секунду). Для быстро обновляющихся данных увеличьте частоту до `<fps:100>` (100 раз в секунду).

> **О цвете и шрифте:** Эти теги работают самостоятельно (без `<md>`) и могут быть вложенными. Пути шрифтов поддерживают абсолютные пути, переменные окружения или пути относительно директории плагина.

---

## Поддерживаемые языки

Python, PowerShell, Batch, JavaScript... даже Shell, Ruby, PHP, Lua и **более 90 языков** поддерживаются! Пока у вас установлен интерпретатор, любой язык работает.

> **Рекомендуется:** Используйте **PowerShell (.ps1)** или **Batch (.bat)** — встроены в Windows, установка не требуется, меньше потребление ресурсов.

---

## Это безопасно?

При первом запуске плагина Catime спросит:

- **Отмена** = Не запускать
- **Запустить один раз** = Запустить только сейчас, спросит снова в следующий раз
- **Доверять и запустить** = Всегда запускать автоматически

Если вы измените файл плагина, Catime спросит снова для предотвращения подмены.

---

## Часто задаваемые вопросы

### Плагин не показывает контент?

Проверьте:
- Путь к файлу правильный (скрипт должен записывать в `output.txt` в той же директории)
- Интерпретатор установлен (например, скрипты Python требуют установленный Python)

### Как остановить плагин?

Правый клик на значке → Плагины → Нажмите снова на работающий плагин (отмечен ✓)

### Нужен перезапуск после редактирования?

Нет! Catime автоматически обнаруживает изменения и перезапускает плагин (горячая перезагрузка).

### Можно запустить несколько плагинов?

Нет, только один за раз. Нажмите на другой плагин для переключения; текущий остановится автоматически.

### Плагины продолжают работать после закрытия Catime?

Нет. Catime останавливает все процессы плагинов при закрытии.

---

## Примечания

⚠️ **Избегайте вложенных подпроцессов**

Используйте один процесс для выполнения задач. Если ваш скрипт создаёт подпроцессы (например, используя `start` в `.bat`), они могут не очиститься корректно.

---

**Вот и всё! Теперь создайте свой первый плагин!** 🚀
